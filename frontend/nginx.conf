# nginx.conf
server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /uploads/ {
        proxy_pass http://host.docker.internal:3003/uploads/;
    }
}